{
  "name": "fsvng-site",
  "version": "1.0.0",
  "description": "ФСВНГ сайт с безопасной архитектурой",
  "type": "module",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "firebase": "^10.8.0",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  }
}
